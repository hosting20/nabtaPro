import ProjectsGrid from './ProjectsGrid.jsx';

export const dynamic = 'force-dynamic';
export const metadata = { title: 'مشاريعي — نبتة' };

export default function ProjectsPage() {
  return <ProjectsGrid />;
}
